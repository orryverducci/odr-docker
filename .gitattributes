# Auto detect text files and perform LF normalization
* text=auto

# Set files to be explicitly interpreted as text,
# plus any specific settings
*.md text
*.sh text eol=lf
.gitattributes text
.gitignore text

# Don't export git files
.gitattributes export-ignore
.gitignore export-ignore
